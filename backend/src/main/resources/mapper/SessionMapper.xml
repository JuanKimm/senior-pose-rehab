<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.seniorrehab.repository.SessionMapper">

    <!-- 운동 세션 시작 -->
    <insert id="insertSession" useGeneratedKeys="true" keyProperty="sessionId">
        INSERT INTO EXERCISE_SESSION (user_id, exercise_type_id, exercise_date, total_count, duration_sec)
        VALUES (#{userId}, #{exerciseTypeId}, CURDATE(), 0, 0)
    </insert>

</mapper>